---
pageable: true
per_page: 50
---
<% if paginate && num_pages > 1 %>
  <p>Page <%= page_number %> of <%= num_pages %></p>

  <% if prev_page %>
    <p><%= link_to 'Previous page', prev_page %></p>
  <% end %>
<% end %>

<% page_articles.each_with_index do |article, i| %>
  <% if i == 0  %>
     <h2>current → <%= link_to article.title, article %></h2>&nbsp;&nbsp;<span><%= article.date.strftime('%b %e') %></span>
     <br>
  <% elsif i == 1 %>
    <h2>older ↓ <%= link_to article.title, article %></h2>&nbsp;&nbsp;<span><%= article.date.strftime('%b %e') %></span>
    <br>
  <% else %>
    <h2><%= link_to article.title, article %></h2>&nbsp;&nbsp;<span><%= article.date.strftime('%b %e') %></span>
    <br>
  <% end %>
<% end %>

<% if paginate %>
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %>
<% end %>
<br>
<br>
<br>
<!-- <a href="type">Or browse by type?</a> -->
